pluginManagement {
    repositories {
        // Google's Maven mirror first - avoids Maven Central 403s in Cloud Build
        maven { url 'https://maven-central.storage.googleapis.com' }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'gateway-service'
