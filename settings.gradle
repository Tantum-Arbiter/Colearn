pluginManagement {
    repositories {
        maven { url 'https://maven-central.storage.googleapis.com' }  // Google mirror (faster, avoids Maven Central 403s)
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'colearn'

include 'gateway-service'
include 'func-tests'
include 'nft'