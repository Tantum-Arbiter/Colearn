{
    "sql_injections": [
        { "token": "' OR 1=1--", "description": "Basic SQL injection" },
        { "token": "'; DROP TABLE users;--", "description": "SQL drop table injection" },
        { "token": "1' OR '1'='1", "description": "SQL boolean injection" },
        { "token": "admin'--", "description": "Comment injection" },
        { "token": "1; SELECT * FROM users", "description": "Stacked query injection" },
        { "token": "' UNION SELECT * FROM users--", "description": "Union injection" }
    ],
    "xss_payloads": [
        "<script>alert('XSS')</script>",
        "<img src=x onerror=alert('XSS')>",
        "javascript:alert('XSS')",
        "<svg onload=alert('XSS')>",
        "'\"><script>alert('XSS')</script>",
        "<body onload=alert('XSS')>"
    ],
    "command_injection": [
        "; ls -la",
        "| cat /etc/passwd",
        "`whoami`",
        "$(id)",
        "; rm -rf /"
    ],
    "path_traversal": [
        "../../../etc/passwd",
        "....//....//....//etc/passwd",
        "%2e%2e%2f%2e%2e%2f%2e%2e%2fetc%2fpasswd",
        "..\\..\\..\\windows\\system32\\config\\sam"
    ],
    "ssti_payloads": [
        "${7*7}",
        "{{7*7}}",
        "${T(java.lang.Runtime).getRuntime().exec('id')}",
        "#{7*7}"
    ],
    "header_injection": [
        "test\r\nX-Injected: header",
        "test%0d%0aX-Injected:%20header",
        "test\nSet-Cookie: malicious=true"
    ]
}

