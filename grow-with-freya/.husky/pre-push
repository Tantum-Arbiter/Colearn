#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-push validation for grow-with-freya..."

# Change to grow-with-freya directory and run full validation
cd grow-with-freya && npm run validate
